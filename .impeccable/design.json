{
  "schemaVersion": 2,
  "generatedAt": "2026-09-06T00:52:15Z",
  "title": "Design System: Riso Notebook",
  "extensions": {
    "colorMeta": {
      "brand": {
        "role": "primary",
        "displayName": "Editorial Magenta",
        "canonical": "#D63A86",
        "dark": "#F06AA6",
        "tonalRamp": [
          "#3D1226",
          "#5E1B3A",
          "#822650",
          "#A82E67",
          "#D63A86",
          "#E85CA0",
          "#F189BE",
          "#F9C2DC"
        ]
      },
      "teal": {
        "role": "secondary",
        "displayName": "Registration Teal",
        "canonical": "#2AA7C8",
        "dark": "#5CC7E8",
        "tonalRamp": [
          "#0A2E38",
          "#114654",
          "#185F72",
          "#1F8299",
          "#2AA7C8",
          "#55BCD6",
          "#8AD3E5",
          "#C2E9F2"
        ]
      },
      "paper": {
        "role": "neutral",
        "displayName": "Bone Paper",
        "canonical": "#F6F1E6",
        "dark": "#191712",
        "tonalRamp": [
          "#232019",
          "#6C6759",
          "#D9D3C6",
          "#E9E5DC",
          "#F3EFE6",
          "#F6F1E6"
        ]
      },
      "ink": {
        "role": "neutral",
        "displayName": "Ink",
        "canonical": "#232019",
        "dark": "#EDE8DC",
        "tonalRamp": [
          "#232019",
          "#6C6759",
          "#D9D3C6",
          "#E9E5DC",
          "#F3EFE6",
          "#F6F1E6"
        ]
      },
      "ink-secondary": {
        "role": "neutral",
        "displayName": "Secondary Ink",
        "canonical": "#6C6759",
        "dark": "#A8A18E"
      },
      "positive": {
        "role": "semantic",
        "displayName": "Live Green",
        "canonical": "#4E7A44",
        "dark": "#9DBB7F"
      },
      "warning": {
        "role": "semantic",
        "displayName": "WIP Ochre",
        "canonical": "#9A6516",
        "dark": "#E0A33E"
      },
      "error": {
        "role": "semantic",
        "displayName": "Error Red",
        "canonical": "#C0392B",
        "dark": "#E86254"
      }
    },
    "typographyMeta": {
      "display": {
        "displayName": "Display",
        "purpose": "The one hero headline, on the 404 only. Merriweather 800, 3px ghost."
      },
      "masthead": {
        "displayName": "Masthead",
        "purpose": "The rail wordmark on every page — \"Gaspery.\" at Merriweather 400, 38px (32px ≤1000px), on a 2px magenta ghost. Class .rail-wordmark, not .wordmark."
      },
      "headline": {
        "displayName": "Headline",
        "purpose": "Section headings (Writing, Apps). Merriweather 700."
      },
      "title": {
        "displayName": "Title",
        "purpose": "Content titles — post rows, app names, the app-page wordmark, subscribe line. Merriweather 700."
      },
      "body": {
        "displayName": "Body",
        "purpose": "Intro and body copy (17px); dek is the 15px secondary-ink variant. Inter 400."
      },
      "label": {
        "displayName": "Label",
        "purpose": "Eyebrows, meta lines, status pills. Inter 600, uppercase, 0.14em tracking."
      }
    },
    "shadows": [],
    "motion": [
      {
        "name": "hover-color",
        "value": "color 0.15s ease",
        "purpose": "Link, heading, and nav hover shift to brand-bright. The only recurring transition."
      }
    ],
    "breakpoints": [
      {
        "name": "collapse",
        "value": "1000px",
        "purpose": "Two-column rail + main collapses to a single stacked column; the rail masthead steps from 38px to 32px."
      },
      {
        "name": "tablet-band",
        "value": "861px–1000px",
        "purpose": "Inside the collapsed range: a centered 768px notebook column with 56px/32px padding; the 404 Hero at 40px."
      },
      {
        "name": "mobile-sm",
        "value": "420px",
        "purpose": "Type-only step; shrinks the 404's Hero from 34px to 30px."
      }
    ],
    "accessibility": [
      {
        "name": "reduced-transparency",
        "value": "prefers-reduced-transparency: reduce",
        "purpose": "Hides the misregistration ghost and halftone; blend effects drop to solid ink."
      },
      {
        "name": "increased-contrast",
        "value": "prefers-contrast: more",
        "purpose": "Same gate as reduced-transparency — meaning never depends on a blend mode."
      }
    ]
  },
  "components": [
    {
      "name": "Monogram",
      "kind": "custom",
      "refersTo": "monogram",
      "description": "The identity mark — a magenta ink square carrying the G, with a misregistered teal ghost behind it. Reused by app-page headers with the app's initial.",
      "html": "<div class='ds-monogram'><span class='ds-monogram-ghost'></span><span class='ds-monogram-ink'>G</span></div>",
      "css": ".ds-monogram { position: relative; width: 46px; height: 46px; } .ds-monogram-ghost, .ds-monogram-ink { position: absolute; inset: 0; width: 46px; height: 46px; border-radius: 2px; } .ds-monogram-ghost { transform: translate(3px, 3px); background: var(--color-teal); mix-blend-mode: multiply; } .ds-monogram-ink { background: var(--color-brand); color: var(--color-paper); display: flex; align-items: center; justify-content: center; font-family: Merriweather, Georgia, serif; font-weight: 900; font-size: 22px; }"
    },
    {
      "name": "New Tag",
      "kind": "chip",
      "refersTo": "tag-new",
      "description": "Marks the newest post. Magenta fill, paper text, tiny uppercase.",
      "html": "<span class='ds-tag-new'>New</span>",
      "css": ".ds-tag-new { display: inline-block; font-family: Inter, sans-serif; font-weight: 700; font-size: 10px; letter-spacing: 0.12em; text-transform: uppercase; color: var(--color-paper); background: var(--color-brand); padding: 2px 6px; border-radius: 2px; }"
    },
    {
      "name": "Status Pill",
      "kind": "chip",
      "refersTo": "status-live",
      "description": "Dot + uppercase label. Color plus text — never color alone. Live (green) / WIP (ochre).",
      "html": "<span class='ds-status ds-status-live'><span class='ds-status-dot'></span>Live</span>",
      "css": ".ds-status { display: inline-flex; align-items: center; gap: 5px; font-family: Inter, sans-serif; font-weight: 600; font-size: 10px; letter-spacing: 0.1em; text-transform: uppercase; } .ds-status-dot { width: 6px; height: 6px; border-radius: 50%; flex-shrink: 0; } .ds-status-live { color: var(--color-positive); } .ds-status-live .ds-status-dot { background: var(--color-positive); }"
    },
    {
      "name": "Nav Item",
      "kind": "nav",
      "refersTo": "nav-item",
      "description": "Rail navigation. Serif, a magenta arrow marker on the active item, brand-bright on hover.",
      "html": "<a class='ds-nav-item' href='#' aria-current='page'><span class='ds-nav-marker'>&#8594;</span>Writing</a>",
      "css": ".ds-nav-item { display: flex; align-items: baseline; gap: 10px; font-family: Merriweather, Georgia, serif; font-weight: 700; font-size: 19px; line-height: 1.15; color: var(--color-ink); text-decoration: none; transition: color 0.15s ease; } .ds-nav-marker { display: inline-block; width: 12px; flex-shrink: 0; color: var(--color-brand); font-family: Inter, sans-serif; font-weight: 700; font-size: 15px; } .ds-nav-item:hover, .ds-nav-item:focus-visible { color: var(--color-brand-bright); }"
    },
    {
      "name": "Registration Mark",
      "kind": "custom",
      "refersTo": "registration-mark",
      "description": "The connective print motif — teal crosshair + magenta ring. Variants: full / cross / ring.",
      "html": "<svg class='ds-reg' width='16' height='16' viewBox='0 0 16 16' fill='none' aria-hidden='true'><path d='M8 0.5V15.5M0.5 8H15.5' stroke='var(--color-teal)' stroke-width='1'/><circle cx='8' cy='8' r='3.2' stroke='var(--color-brand)' stroke-width='1'/></svg>",
      "css": ".ds-reg { display: block; }"
    },
    {
      "name": "Writing Post Row",
      "kind": "card",
      "refersTo": "writing-post-row",
      "description": "A blog entry in the Writing stream — meta line, serif title that hovers magenta, dek. Rows over cards.",
      "html": "<a class='ds-post' href='#'><div class='ds-post-meta'><span class='ds-label'>18 JUL 2026</span><span class='ds-post-sep'>/</span><span class='ds-label'>7 min</span><span class='ds-post-tag'>New</span></div><h3 class='ds-post-title'>The two-ink discipline</h3><p class='ds-post-dek'>What a Risograph's two colours taught me about restraint.</p></a>",
      "css": ".ds-post { display: block; padding: 22px 0; border-bottom: 1px solid var(--color-hairline); text-decoration: none; } .ds-post-meta { display: flex; align-items: center; gap: 8px; margin-bottom: 9px; } .ds-label { font-family: Inter, sans-serif; font-weight: 600; font-size: 11px; letter-spacing: 0.14em; text-transform: uppercase; color: var(--color-ink-secondary); } .ds-post-sep { color: var(--color-hairline); font-size: 11px; } .ds-post-tag { font-family: Inter, sans-serif; font-weight: 700; font-size: 10px; letter-spacing: 0.12em; text-transform: uppercase; color: var(--color-paper); background: var(--color-brand); padding: 2px 6px; border-radius: 2px; } .ds-post-title { margin: 0; font-family: Merriweather, Georgia, serif; font-weight: 700; font-size: 21px; line-height: 1.28; color: var(--color-ink); transition: color 0.15s ease; } .ds-post:hover .ds-post-title { color: var(--color-brand-bright); } .ds-post-dek { margin: 7px 0 0; font-family: Inter, sans-serif; font-size: 15px; line-height: 1.53; color: var(--color-ink-secondary); max-width: 440px; }"
    },
    {
      "name": "Rail Masthead",
      "kind": "custom",
      "refersTo": "rail-wordmark",
      "description": "The site's masthead on every page: \"Gaspery.\" at Merriweather 400, 38px, on a 2px magenta ghost, over the tagline label. Regular weight because the G plate above it carries the heavy mark; 2px because at this weight 3px reads as an outline. The ink copy is the homepage's h1.",
      "html": "<a class='ds-masthead' href='#'><span class='ds-masthead-wordmark'><span class='ds-masthead-ghost' aria-hidden='true'>Gaspery.</span><span class='ds-masthead-ink'>Gaspery.</span></span><span class='ds-masthead-label'>Words, design, tools &amp; links</span></a>",
      "css": ".ds-masthead { display: inline-flex; flex-direction: column; gap: 6px; text-decoration: none; color: var(--color-ink); } .ds-masthead-wordmark { position: relative; display: block; } .ds-masthead-ink, .ds-masthead-ghost { display: block; font-family: Merriweather, Georgia, serif; font-weight: 400; font-size: 38px; line-height: 1.1; letter-spacing: -0.01em; } .ds-masthead-ink { position: relative; z-index: 1; transition: color .15s ease; } .ds-masthead-ghost { position: absolute; inset: 0; transform: translate(2px, 2px); color: var(--color-brand); mix-blend-mode: multiply; pointer-events: none; } .ds-masthead:hover .ds-masthead-ink { color: var(--color-brand-bright); } .ds-masthead-label { font-family: Inter, sans-serif; font-weight: 600; font-size: 11px; letter-spacing: .14em; text-transform: uppercase; color: var(--color-ink-secondary); } @media (prefers-color-scheme: dark) { .ds-masthead-ghost { mix-blend-mode: screen; } } @media (prefers-reduced-transparency: reduce), (prefers-contrast: more) { .ds-masthead-ghost { display: none; } }"
    },
    {
      "name": "Hero Misregistration (404)",
      "kind": "custom",
      "refersTo": "hero-title",
      "description": "The 404's headline — Merriweather 800 riding a 3px magenta ghost in multiply. Ordinary pages carry the Rail Masthead instead; never both on one page. The ghost hides under the accessibility gate.",
      "html": "<div class='ds-hero-title'><h1 class='ds-hero-ghost' aria-hidden='true'>Blank page.</h1><h1 class='ds-hero-ink'>Blank page.</h1></div>",
      "css": ".ds-hero-title { position: relative; max-width: 760px; } .ds-hero-ink, .ds-hero-ghost { margin: 0; font-family: Merriweather, Georgia, serif; font-weight: 800; font-size: 56px; line-height: 1.1; letter-spacing: -0.02em; color: var(--color-ink); } .ds-hero-ghost { position: absolute; inset: 0; transform: translate(3px, 3px); color: var(--color-brand); mix-blend-mode: multiply; z-index: -1; pointer-events: none; } @media (prefers-reduced-transparency: reduce), (prefers-contrast: more) { .ds-hero-ghost { display: none; } }"
    },
    {
      "name": "Halftone Dot Field",
      "kind": "custom",
      "refersTo": "halftone",
      "description": "Teal dot screen that fades out radially, bled off the corner of the rail's identity block (or the 404's hero). Decorative only; multiply on light, screen on dark. Shown on a bone backing so the blend reads.",
      "html": "<div class='ds-halftone-demo'><div class='ds-halftone' aria-hidden='true'></div></div>",
      "css": ".ds-halftone-demo { display: inline-block; padding: 12px; background: var(--color-paper); } .ds-halftone { width: 320px; height: 200px; background-image: radial-gradient(var(--color-teal) 1.1px, transparent 1.5px); background-size: 7px 7px; mix-blend-mode: multiply; -webkit-mask-image: radial-gradient(120% 120% at 92% 8%, #000 0%, rgba(0,0,0,.5) 34%, transparent 68%); mask-image: radial-gradient(120% 120% at 92% 8%, #000 0%, rgba(0,0,0,.5) 34%, transparent 68%); } @media (prefers-color-scheme: dark) { .ds-halftone { mix-blend-mode: screen; } }"
    },
    {
      "name": "Now Panel",
      "kind": "custom",
      "refersTo": "now-panel",
      "description": "Closes the Apps stream: Building/Reading/Watching/Listening rows, each a teal uppercase row-label over a value line, hairline dividers between rows, a closing link through to /now.",
      "html": "<div class='ds-now'><div class='ds-now-head'><span class='ds-now-dot'></span><span class='ds-now-label'>Now</span></div><div class='ds-now-row'><span class='ds-now-row-label'>Building</span><span class='ds-now-row-value'>Teaching GRØD to spot meetings on its own.</span></div><div class='ds-now-row ds-now-row--divided'><span class='ds-now-row-label'>Reading</span><span class='ds-now-row-value'>The Faith of Beasts — James S. A. Corey</span></div><a class='ds-now-link' href='#'>More on all this &#8594;</a></div>",
      "css": ".ds-now { display: flex; flex-direction: column; gap: 12px; padding-top: 18px; border-top: 1px solid var(--color-hairline); max-width: 340px; } .ds-now-head { display: flex; align-items: center; gap: 8px; } .ds-now-dot { width: 7px; height: 7px; border-radius: 50%; background: var(--color-teal); } .ds-now-label { font-family: Inter, sans-serif; font-weight: 600; font-size: 11px; letter-spacing: 0.14em; text-transform: uppercase; color: var(--color-ink-secondary); } .ds-now-row { display: flex; flex-direction: column; gap: 3px; } .ds-now-row--divided { padding-top: 11px; border-top: 1px solid var(--color-hairline); } .ds-now-row-label { font-family: Inter, sans-serif; font-weight: 600; font-size: 10px; letter-spacing: 0.12em; text-transform: uppercase; color: var(--color-teal); } .ds-now-row-value { font-family: Inter, sans-serif; font-size: 15px; line-height: 1.5; color: var(--color-ink); } .ds-now-link { font-family: Inter, sans-serif; font-weight: 600; font-size: 13px; color: var(--color-brand); text-decoration: none; } .ds-now-link:hover { color: var(--color-brand-bright); }"
    },
    {
      "name": "Riso-Duotone Photo",
      "kind": "custom",
      "refersTo": "riso-photo",
      "description": "The treatment for real photography — grayscale piped through an ink/paper duotone filter (SVG, defined once in Base.astro), then the halftone dot recipe laid full-bleed over the top. A photo run through the same press as everything else.",
      "html": "<figure class='ds-riso-photo'><div class='ds-riso-photo-img' role='img' aria-label='Duotone-treated photograph'></div></figure>",
      "css": ".ds-riso-photo { margin: 0; position: relative; border-radius: 8px; overflow: hidden; } .ds-riso-photo-img { width: 280px; height: 180px; background: linear-gradient(135deg, var(--color-ink), var(--color-paper)); } .ds-riso-photo::after { content: ''; position: absolute; inset: 0; background-image: radial-gradient(var(--color-teal) 1.1px, transparent 1.5px); background-size: 7px 7px; mix-blend-mode: multiply; } @media (prefers-color-scheme: dark) { .ds-riso-photo::after { mix-blend-mode: screen; } }"
    }
  ],
  "narrative": {
    "northStar": "The Proof Sheet",
    "overview": "A proof sheet is the test pull a printer takes off the press to check the registration and ink before committing to the run — the sheet where the craft is right there to see. That's this system: two spot inks, magenta and teal, on a warm bone ground, printed slightly out of register. The constraint is the whole point — with only two colors you stop reaching for a third to solve a hierarchy problem and solve it with weight, size, and space instead. Restraint is the method, not an absence of ideas. As `PRODUCT.md` puts it, \"the craft is the credential\": the system earns trust by being visibly, precisely made.\n\nThe feel is quiet, exact, crafted, and warmly personal — a working notebook, not a marketing site. Density is generous: a lot of bone whitespace, a two-column layout (a sticky identity rail beside a wide main column), and content set in rows divided by hairlines rather than boxed into cards. The one loud moment per page is the misregistered wordmark in the rail (the hero headline, on the 404); everything else is composed and calm. Print character carries the warmth — ink, paper, and registration marks — not a tinted-neutral \"cozy\" background.\n\nWhat it explicitly rejects (from `PRODUCT.md`'s anti-references): the generic SaaS/startup landing (gradient hero, feature grid, \"Get started free\"); the salesy funnel (pop-ups, urgency, subscribe-walls, growth hacks); the sterile minimalist portfolio template (a cold Dribbble/Behance grid with no point of view); and anything loud or maximalist. Nothing that trades craft or restraint for reach.",
    "keyCharacteristics": [
      "Two inks, one ground, in chrome. Magenta + teal over bone paper. No third accent in chrome, ever — a feature study may still print a subject's own ink as its subject matter (see The Identity Exception, §2).",
      "Serif prints, sans functions. Merriweather for anything that should read as printed; Inter for everything operational.",
      "Flat by tone, not shadow. Depth comes from `paper → surface → surface-raised` layering and hairlines; there are no box-shadows.",
      "One loud moment. The misregistered headline is spent once per page — the rail masthead everywhere, the Hero on the 404 — the rest is quiet.",
      "Accessibility-gated ink. Every blend-mode effect degrades to solid ink under `prefers-reduced-transparency` / `prefers-contrast: more`; meaning never depends on a blend."
    ],
    "rules": [
      {
        "name": "The Two-Ink Rule",
        "body": "Every screen's chrome prints in exactly two inks — magenta and teal — over one bone ground. A third accent color is never introduced into chrome; hierarchy there is solved with weight, size, and space. Teal is the rarer ink, and it comes in two weights of the same colour: `--color-teal` for marks that are *printed* rather than read — halftone, ghost, registration marks, status dots — and `--color-teal-ink` for the small set of labels and markers that are *read*: enumerations, field labels, attributions, and printer's marks that happen to carry a link. Teal is never body copy, and never an ordinary hyperlink. Magenta remains the link ink.",
        "section": "colors"
      },
      {
        "name": "The Identity Exception",
        "body": "A third ink may appear on a page, but only inside a study whose subject *is* that identity — never in site chrome. A study supplies that ink itself, as content, rather than the stylesheet registering it as a token: a `Spec` item's `ink` field takes either a named site token (`brand`, `teal`) or a literal hex, with an optional `inkDark` as the dark-scheme counterpart for a hex that would not survive the ground inverting — omit it and `ink` is used in both. GRØD's own third ink is the first instance: its Direction glyph carries `ink: \"#48234F\"`, `inkDark: \"#C79BD0\"` inline in the post's own content, not a token declared anywhere in CSS. A study's ink surfaces only as the subject itself — described in prose, printed as one of the app's own colours in a `Swatches` chip, or used to tint a `Spec` glyph when a reading is specifically about that identity's colour. Chrome — the rail, the meta row, the imprint, every page outside a study — stays two inks, always.",
        "section": "colors"
      },
      {
        "name": "Specimen Grounds",
        "body": "`--specimen-light` / `--specimen-light-ink` / `--specimen-dark` / `--specimen-dark-ink` (`src/styles/feature.css`) are the two fixed grounds a `Glyphs` strip renders its marks against — a menu bar, a phone wallpaper, whatever environment the study is proving a mark inside. Like a `Swatches` chip, they're content a study supplies, not site chrome, and they are deliberately theme-invariant: a strip labelled \"Light\" has to read as light no matter the reader's own theme, so these four tokens must **never** gain a `prefers-color-scheme` or `[data-theme]` override — themed values here previously made a \"Light\" strip render dark for a reader in dark mode, a label contradicting the thing it labels. Don't confuse them with the site's own `--color-surface` / `--color-ink` chrome tokens, which are theme-aware by design.",
        "section": "colors"
      },
      {
        "name": "The Token Rule",
        "body": "Dark values are theme-aware by default. Never hardcode a light value where a token exists — the token already carries its dark twin.",
        "section": "colors"
      },
      {
        "name": "The Printed-Heading Rule",
        "body": "Serif (Merriweather) is reserved for what should read as printed — display, headings, titles, the wordmark. Sans (Inter) carries everything functional — body, meta, labels, UI. The two are never swapped, and a heading is never set in sans \"to be modern.\"",
        "section": "typography"
      },
      {
        "name": "The Single-Impression Rule",
        "body": "The misregistration is spent once per page — the rail masthead everywhere at 2px, the Hero on the 404 at 3px, never both on the same page. (The G plate's own 3px teal ghost is a printed mark, not a headline, and sits outside this count.) Its power is scarcity: a second offset headline of equal weight halves the impact of both. Break it only on purpose — a distinct page *type*, or a spread where a second region genuinely earns its own loud moment — never as decoration or reflex. The test: if you can't say what the second impression is *for*, it stays in register.",
        "section": "typography"
      },
      {
        "name": "The Flat Press Rule",
        "body": "Surfaces never lift off the page. If something needs to feel separate, change its tonal layer (`surface` / `surface-raised`) or divide it with a hairline — never add a shadow. The only apparent \"offset\" allowed is the misregistration, and that is ink, not elevation.",
        "section": "elevation"
      }
    ],
    "dos": [
      "Do reuse an existing token before adding a new one. If nothing fits, that's a real gap — add it here and to `global.css` together, never leave it implicit in one component's inline values.",
      "Do set anything printed (display, headings, titles, wordmark) in Merriweather and anything functional (body, meta, labels) in Inter.",
      "Do let new page *types* break from the two-column rail layout (the GRØD app page is the first test) — but keep the same palette, type scale, and signature motifs. \"Different structure, same ink,\" never \"different structure, different system.\"",
      "Do check both color schemes and the accessibility gates (`prefers-reduced-transparency`, `prefers-contrast: more`) before calling any illustrated component done.",
      "Do pair every status color with a text label — color is never the only signal."
    ],
    "donts": [
      "Don't introduce a third ink or accent color into chrome. Two inks and a ground there; solve hierarchy with weight, size, and space — a study's own subject-matter ink is the one sanctioned exception (see The Identity Exception, §2).",
      "Don't add box-shadows, glows, or glassmorphism — the system is flat (see The Flat Press Rule).",
      "Don't scatter the misregistration across headlines by reflex — spend it once by default (see The Single-Impression Rule), and never let meaning depend on a blend mode.",
      "Don't hardcode a light color value where a token exists — the token carries its dark twin.",
      "Don't build a generic SaaS/startup landing: gradient hero, feature grid, \"Get started free.\"",
      "Don't add salesy-funnel patterns — pop-ups, urgency, subscribe-walls, growth hacks.",
      "Don't flatten this into a sterile minimalist portfolio grid with no point of view, and don't go loud or maximalist. Nothing that trades craft or restraint for reach.",
      "Don't use a colored `border-left`/`border-right` stripe as an accent, or gradient text (`background-clip: text`) — neither belongs in this system or any."
    ]
  }
}
